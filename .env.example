# =============================================================================
# EXISTING (already on Render — keep these)
# =============================================================================
# MONGO_URI=
# MONGO_DB=
# PORT=10000
# FRONTEND_ORIGIN=
# GOOGLE_SCRIPT_URL=
# RESUME_LINK=
# TRACKING_BASE_URL=
# GMAIL_ID=
# GMAIL_APP_PASSWORD=
# IMAP_SERVER=
# IMAP_PORT=
# GEMINI_API_KEY=
# DASHBOARD_ID=
# DASHBOARD_PASSWORD=
# BOUNCE_WEBHOOK_SECRET=
# EMAIL_SUBJECT / EMAIL_TEMPLATE_HTML / FOLLOWUP_* / BREAKUP_*

# =============================================================================
# NEW — Internship Link Intelligence (optional unless you want digests)
# Same MongoDB is reused. New collections: companies, jobs, crawler_runs,
# notifications_log
# =============================================================================

# Protect POST /api/v1/scheduler/tick (recommended on Render)
SCHEDULER_SECRET=

# Optional daily digests (leave empty to only use dashboard)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DISCORD_WEBHOOK_URL=
SLACK_WEBHOOK_URL=
